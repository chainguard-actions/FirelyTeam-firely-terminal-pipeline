name: Test - FIRELY_TERMINAL_VALIDATOR_ENGINE input coverage
on:
  workflow_dispatch:

jobs:
  test-validator-engine:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create minimal package.json
        run: |
          cat > "$GITHUB_WORKSPACE/package.json" << 'EOF'
          {
            "name": "test-fhir-project",
            "version": "1.0.0",
            "fhirVersions": ["4.0.1"],
            "dependencies": {}
          }
          EOF

      - id: action
        name: Run action with FIRELY_TERMINAL_VALIDATOR_ENGINE set (both validators disabled)
        uses: .
        with:
          PATH_TO_CONFORMANCE_RESOURCES: tests/fixtures/fhir-r4/conformance
          DOTNET_VALIDATION_ENABLED: false
          JAVA_VALIDATION_ENABLED: false
          SUSHI_ENABLED: false
          SIMPLIFIER_USERNAME: test@example.com
          SIMPLIFIER_PASSWORD: test-password
          FIRELY_TERMINAL_VALIDATOR_ENGINE: firely

      - name: Assert action completed successfully
        run: |
          echo "PASS: Action completed with FIRELY_TERMINAL_VALIDATOR_ENGINE=firely (validators disabled, engine config skipped)"
